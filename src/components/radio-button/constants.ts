import {ColorLight} from '@theme/colors';
import {HairlineWidth} from '@utils/constant';
import {scale} from '@utils/scale';

export const SIZE = scale(30);

export const ACTIVE_COLOR = ColorLight.DarkViolet;

export const UN_ACTIVE_COLOR = ColorLight.DarkGray;

export const STROKE_WIDTH = HairlineWidth * 5;

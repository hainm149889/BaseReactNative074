import {SLICE_NAME} from '@redux/action-slice/constant';

export const INCREMENT = SLICE_NAME.COUNTER + 'INCREMENT';
export const DECREMENT = SLICE_NAME.COUNTER + 'DECREMENT';
export const INCREMENT_BY_AMOUNT = SLICE_NAME.COUNTER + 'INCREMENT_BY_AMOUNT';
